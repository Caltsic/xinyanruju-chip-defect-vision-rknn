# Chip Capture GUI

Run from the project root:

```powershell
F:\anaconda\python.exe -m tools.chip_capture_gui
```

The GUI saves clean frames, annotated frames, and metadata under the selected output directory.
